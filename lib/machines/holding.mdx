export const metadata = {
  title: "Holding",
};

## Holding

This state machine handles holding logic.

### Initial status

We start off in <State>noHolding</State> state and <Context stringify>assumed</Context> and <Context stringify>previous_holding</Context>
